cmake_minimum_required(VERSION 3.20)
project(socom_ee_recovery_tools LANGUAGES CXX)
add_executable(recover_scheduler_player recover_scheduler_player.cpp)
target_compile_features(recover_scheduler_player PRIVATE cxx_std_17)
if(MSVC)
  target_compile_options(recover_scheduler_player PRIVATE /W4 /permissive-)
else()
  target_compile_options(recover_scheduler_player PRIVATE -Wall -Wextra -Wpedantic)
endif()
